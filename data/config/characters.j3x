float player_max_speed=700.0f

int player_max_health=160

float enemy_max_speed=500.0f

float enemy_standby_speed=100.0f

int enemy_max_health=20

float gun_offset_x=20.0f

float gun_offset_y=-5.0f

float max_acceleration=1000.0f

float mouse_reaction_speed=100.0f